{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "api.js",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "joi": "^17.4.0",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^3.6.6",
    "mongoose": "^5.12.3"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "start": "node api.js",
    "client": "cd client && npm start",
    "dev": "concurrently -n 'api,client' -c 'red,green'  \"nodemon api.js\" \"npm run client\""
  },
  "author": "Gyaan",
  "license": "ISC"
}
